# E. 派工 Prompt 模板

> 讀者：主對話模型。用法：選對應型態，填空 `{{ }}`，整段作為 subagent prompt。
> 通則：model 與 effort 顯式指定（見 `C-dispatch.md` §3）；驗收條件必須可機械判定；
> 回報合約每份模板已內建，不要刪。

---

## 模板 1：搜尋 / 定位（建議 model: 小級）

```
【目標】在 {{範圍，如 repo 根目錄 / 某網站}} 找出 {{找什麼}}。
【動機】{{為什麼要找，找到後會拿來做什麼——這決定你該回報到什麼粒度}}。
【做法約束】用 grep/glob/搜尋工具定位，只讀命中處前後 50 行，勿整檔載入。
【驗收條件】
- 每個命中點以「檔案:行號 + 一句話說明」列出。
- 若搜尋 0 命中：回報用過的每個關鍵字/pattern，並給 2 個下一步建議。
【回報格式】命中清單（最多 {{N}} 條，超過則落檔 {{路徑}} 只回摘要）。禁止貼大段原文。
```

## 模板 2：實作（建議 model: 中級）

```
【目標】{{做什麼功能/修什麼 bug，一句話}}。
【動機】{{使用者要解決的真問題——細節模糊時據此取捨}}。
【範圍】只允許改動：{{檔案/目錄清單}}。需要改清單外的檔 → 停下回報，不要直接改。
【驗收條件】
- {{測試指令}} 全數通過（新功能需附新測試，至少 {{N}} 個案例含 1 個邊界案例）。
- {{lint/type check 指令}} 通過。
- {{其他可判定條件，如「public 函式有 docstring」}}。
【回報格式】改動檔案清單（檔案:行號範圍 + 各一句話）、測試 pass/fail 統計、
所做假設清單、已知限制。失敗案例只貼 stack trace 前 5 行。禁止貼完整 diff。
```

## 模板 3：重構（建議 model: 中級）

```
【目標】把 {{對象}} 從 {{現狀}} 重構為 {{目標形態}}。
【動機】{{為什麼重構，例如降低耦合以便下一步做 X}}。
【硬約束】行為不變：重構前先跑 {{測試指令}} 記錄基線結果；重構後結果必須與基線一致。
基線本身就有失敗 → 停下回報，不要順手修（修 bug 是另一個任務）。
【範圍】{{允許改動的檔案清單}}。
【驗收條件】
- 測試結果 = 基線結果。
- {{結構性判準，如「circular import 數量從 N 降到 0」「該類別行數 < 200」}}。
【回報格式】基線 vs 重構後測試統計、改動檔案清單（檔案:行號）、
未竟事項（發現但不在範圍內的問題，列出不處理）。
```

## 模板 4：研究 / 資料蒐集（建議 model: 中級；純彙整可小級）

```
【問題】{{要回答的具體問題，疑問句}}。
【動機】{{這答案會用來做什麼決策}}。
【來源優先序】{{如：官方文件 > 原始碼 > issue 討論 > 部落格}}。至少交叉 2 個獨立來源。
【驗收條件】
- 直接回答問題（BLUF），每個關鍵主張標【實/推/臆】並附來源（URL 或 檔案:行號）。
- 來源互相矛盾時：並列雙方說法與各自依據，不自行裁決。
- 查不到 → 明說查不到 + 試過的查法，禁止編造。
【回報格式】結論 ≤5 行；完整筆記落檔 {{路徑}}；來源清單。
```

## 模板 5：審查 / 驗收（建議 model: 依 C-dispatch §6 選）

```
【你是 fresh-context 驗收者】只依據下方產出物與驗收條件判定，
你沒有、也不需要執行過程的任何背景。
【產出物】{{檔案路徑清單}}。
【驗收條件】
{{逐條列出，與當初派工時完全相同的清單}}
【做法】
- 檔案類：逐一 read-back，確認存在、完整、符合條件。
- 程式碼類：實跑 {{測試/執行指令}}，以輸出為準，不以閱讀程式碼「看起來對」為準。
【回報格式】逐條 PASS/FAIL；每個 FAIL 附具體證據（檔案:行號 或 錯誤輸出前 5 行）
與一句話修正建議。你只判定，不動手修。
```

---

## 反模式（看到自己在寫這些就停）

- 「幫我看一下 X」——沒有驗收條件，subagent 會回一篇沒人要的散文。
- 把主對話的整段歷史貼給 subagent——它只需要三件套，多給 = 汙染 + 漏 token。
- 一個 prompt 塞兩個任務——拆成兩次派工，否則回報無法逐條驗收。

## Changelog
2026-07-06 建立/校準（安裝 session）
