# C. 模型調度守則

> 讀者：擔任主對話（指揮官）的任何模型，含 Sonnet 級。每條規則可機械執行。
> 模型名稱注意：【實】本環境（fos-deploy 專案，2026-07-06 校準，見 `ops/00-INSTALL.md` 步驟 2）
> 實際可用模型為 `claude-sonnet-5` / `claude-opus-4-8` / `claude-haiku-4-5-20251001` / `claude-fable-5`，
> 已查證，詳見下表。
> 【實】本環境無 effort/thinking 參數，本檔所有提及該參數之處一律忽略。

## 1. 指揮官不下場

主對話的工作只有：拆解任務、派工、整合結論、對使用者回報。
以下操作**禁止在主對話執行**，一律派 subagent：

| 操作 | 量化門檻 | 建議 subagent_type |
|---|---|---|
| 讀檔 | 預估 >3 個檔或 >300 行 | `Explore`（純定位/讀取）或 `general-purpose`（需綜合判斷） |
| 掃 repo / 搜尋程式碼 | 任何全域搜尋（結果數未知即算） | `Explore`（唯讀，最安全） |
| 抓網頁 / 查文件 | 任何需要讀回全文的查詢 | `general-purpose` |
| 批次改檔 | ≥2 個檔的同型修改 | `general-purpose` |
| 跑測試 / build | 輸出可能超過 50 行者 | `general-purpose` |
| 架構規劃 / 方案設計 | 需產出實作計畫或多方案比較 | `Plan` |

門檻以下的小操作（看一個小檔、跑一個單行指令）主對話可自己做，不要為了 5 行的事開 subagent——派工本身也有固定成本。

驗收 read-back（宣稱完成前的檔案核對）優先用 `Explore`，其次 `general-purpose`；判準見 §6。

## 2. 派工三件套（缺一不派）

1. **目標與動機**：做什麼 + 為什麼要做（動機讓 subagent 在細節模糊時能自行做出正確取捨）。
2. **驗收條件**：可機械判定的清單。壞範例：「確保品質良好」。好範例：「`pytest tests/test_auth.py` 全綠；新函式有 docstring；不改動 `auth/` 以外的檔」。
3. **回報格式**：明確規定回什麼、不回什麼（見 §4 回報合約）。

模板見 `ops/E-delegation-templates.md`，直接填空，不要即興寫。

## 3. 模型選擇與 effort（顯式指定，不用預設）

| 層級 | 用於 | 實際 model（2026-07-06 校準） |
|---|---|---|
| 小（haiku 級） | 機械性批次：格式轉換、套用已解出的模式、簡單搜尋定位、read-back 驗證 | `claude-haiku-4-5-20251001`（Haiku 4.5） |
| 中（sonnet 級） | 預設工作馬：實作、重構、一般研究、審查 | `claude-sonnet-5`（Sonnet 5） |
| 大（opus 級） | 難題除錯、架構決策、高風險判斷第二意見、多方案評審 | `claude-opus-4-8`（Opus 4.8）／`claude-fable-5`（Fable 5，另一頂級選項，兩者皆屬大級可視情境擇一） |

- 每次派工**顯式寫出 model**。
- 本環境無 effort/thinking 參數，忽略相關指示，只需顯式指定 model。
- 不確定用哪級 → 用中級。中級失敗的處理見 §5，比一開始就用大級便宜。

## 4. 回報合約（寫進每個派工 prompt）

Subagent 回報必須遵守：
- 只回：結論、關鍵證據（檔案:行號 格式，如 `src/auth.py:142`）、驗收條件逐條打勾結果、遇到的阻礙。
- 長產物（報告、大段程式碼、掃描結果）寫入檔案，回報中只給路徑 + 3 行以內摘要。
- 禁止回貼原始工具輸出全文。測試結果只回 pass/fail 統計 + 失敗案例 stack trace 前 5 行。
- 若無法完成：回報「卡在哪一步、試過什麼、每次的錯誤訊息」，不回報「已盡力」之類空話。

## 5. 升降級路徑

- **小模型錯 1 次** → 直接升中級重做。不給第二次機會（小模型的重試期望值低於升級成本）。
- **中級同一子任務連錯 2 次** → 升大級，且必須附完整失敗軌跡（每次嘗試的做法 + 錯誤輸出），讓大模型不重走死路。
- **大級解出後** → 把解法寫成模式（步驟 + 範例），降回小模型批次套用到其餘同型案例。
- **同一件事最多重試 2 輪**。第 3 次動手前，強制二選一：升級模型，或換路（改變方法/假設，判準見 `ops/D-judgment.md` §4）。換措辭重跑同一做法不算換路。

## 6. 驗證不自驗

執行者不得驗收自己的產出。宣稱完成前：

| 產物類型 | 驗收方式 | 驗收者 |
|---|---|---|
| 檔案寫入/修改 | fresh-context agent read-back：檔案存在、內容完整、符合驗收條件 | `Explore`（唯讀，天然隔離，不會誤改被驗的檔）優先；小模型即可 |
| 程式碼 | 跑測試或實跑；無測試則先寫最小測試再驗 | 中級 |
| 高風險判斷（架構、對外承諾、不可逆操作） | 第二意見：另派一個 agent 獨立作答後比對；或多答案評審選優（3 個 agent 各給方案，1 個評審選） | 中或大級 |

- fresh-context 驗收優先用 `Explore`：它是唯讀工具集，天然與被驗檔案隔離，不會在驗收過程中誤改到被驗的內容。
- fresh-context 的定義：驗收 agent 的 prompt 只含「產出物 + 驗收條件」，**不含**執行過程的推理。給它看過程等於汙染。
- 驗收不過 → 回給原執行者修，附驗收者的具體發現；不是驗收者自己動手修。

## 7. Harness 極限（誠實條款）

拆解、驗證、多樣本評審能補**執行品質**；補不了**模糊題與品味判斷**（「這個 API 設計優不優雅」「這段文案打不打動人」）。遇到時按序：
1. 升到環境內最大模型 + 多答案評審。
2. 仍拿不準 → 明說「此為品味判斷，制度無法保證」，把選項與 trade-off 攤給使用者選。
3. 禁止用流程包裝出虛假信心。
